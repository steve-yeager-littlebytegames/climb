@model Climb.ViewModels.BaseViewModel

@{
    ViewBag.Title = "Account-Register";
}

<div class="container mt-4">
    <form class="form" asp-action="LogInPost" method="post">
        <img class="mb-4" src="~/images/logo/logo-128x128.png" alt="" width="128" height="128">
        <h1 class="h3 mb-3 font-weight-normal">Please Log In</h1>
        @if (ViewData.ContainsKey("LoginFail"))
        {
            <span>Failed to Log In</span>
        }
        <div class="form-group row">
            <label for="inputEmail" class="col-md-3 col-form-label text-left">Email</label>
            <input type="email" id="inputEmail" name="Email" class="form-control col-md-9" placeholder="Email address" required autofocus>
        </div>
        <div class="form-group row">
            <label for="inputPassword" class="col-md-3 col-form-label text-left">Password</label>
            <input type="password" id="inputPassword" name="Password" class="form-control col-md-9" placeholder="Password" required>
        </div>
        <div class="checkbox" mb-3">
            <label>
                @Html.CheckBox("RememberMe")
                <span>Remember Me</span>
            </label>
        </div>
        <div class="d-flex flex-column align-items-center">
            <button class="btn btn-set btn-lg btn-edit btn-block my-4 mx-auto" type="submit">Log In</button>
            <a asp-action="Register">Register</a>
            <a class="mt-4" asp-action="ForgotPassword">Forgot Password</a>
        </div>
    </form>
</div>